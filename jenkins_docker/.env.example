# 환경 변수 설정 예시
# 사용 방법: 이 파일을 .env로 복사한 후 실제 값으로 변경하세요
# cp .env.example .env

# ngrok Authtoken (jsj_ngrok.yaml 사용 시 필수)
# ngrok.com에서 가입 후 발급받은 authtoken을 입력하세요
# 발급 URL: https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTHTOKEN=your_ngrok_authtoken_here
